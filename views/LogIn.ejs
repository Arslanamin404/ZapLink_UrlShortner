<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - ZapLink -- URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Mulish:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-300 flex flex-col items-center h-screen">
    <%- include("partials/navbar") %>

    <form
      class="flex w-[25rem] mx-auto bg-slate-800 flex-col border border-gray-400 rounded-md p-8 mt-5 text-gray-200"
    >
      <h1
        class="text-3xl p-2 text-center font-semibold border-b border-gray-400"
      >
        LogIn to Your Account
      </h1>
      <div class="my-8 flex flex-col gap-5">
        <div class="w-full">
          <label class="font-bold" for="email">Email</label>
          <input
            class="bg-transparent border border-gray-100 rounded-md outline-none w-full px-3 py-2 mt-1"
            placeholder="yourname@example.com"
            type="email"
            name="email"
            id="email"
            required
          />
          <p id="email_error" class="text-red-500 text-xs mt-2"></p>
        </div>
        <div class="w-full">
          <label class="font-bold" for="password">Password</label>
          <input
            class="bg-transparent border border-gray-100 rounded-md outline-none w-full px-3 py-2 mt-1"
            placeholder="*********"
            type="password"
            name="password"
            id="password"
            required
          />
          <p id="password_error" class="text-red-500 text-xs mt-2"></p>
        </div>
      </div>
      <button
        type="submit"
        class="px-5 py-2 bg-blue-600 rounded-md border-none outline-none"
      >
        LogIn
      </button>
    </form>
    <%- include("partials/footer") %>
  </body>
  <!-- for using axios package -->
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script src="/js/login.js"></script>
</html>
