<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ZapLink - URL Shortener -- Arsalan Rather</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Mulish:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-300 flex flex-col items-center h-screen">
    <%- include("partials/navbar") %>

    <main>
      <h2 class="text-center my-5 text-xl md:text-3xl">
        <strong class="text-teal-700">ZapLink</strong> - URL Shortener
      </h2>
      <div
        class="bg-white rounded-lg p-6 shadow-lg flex flex-col items-center mt-10"
      >
        <% if (locals.shortID) { %>
        <p class="text-lg font-medium mb-4">
          Generated Short URL is:
          <a
            href="https://zaplink-urlshortner.onrender.com/url/<%= shortID %>"
            target="_blank"
            class="text-blue-500 underline hover:text-blue-700"
            >https://zaplink-urlshortner.onrender.com/url/<%= shortID %></a
          >
        </p>
        <% } %>
        <form
          class="flex flex-row justify-center gap-4 items-center w-full"
          method="post"
          action="/url"
        >
          <input
            type="text"
            class="border border-gray-300 rounded-lg px-4 py-2 w-full max-w-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
            name="url"
            placeholder="Enter your original URL"
            required
          />
          <button
            class="bg-blue-500 text-white px-6 py-2 rounded-lg shadow-md hover:shadow-lg hover:bg-blue-600 text-lg"
            type="submit"
          >
            Generate
          </button>
        </form>
      </div>
    </main>
    <%- include("partials/footer") %>
  </body>
</html>
